# ========================================
# DFRM - Application Properties
# ========================================

# Server Configuration
server.port=${PORT:8080}
server.servlet.context-path=/api

# Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/fhs_dev}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:Tr4d1l0s!}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=true

# Redis Configuration
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.timeout=2000ms

# JWT Configuration
jwt.secret=${JWT_SECRET:H5KJhL8MNxcV3BnM4z7P9qRsTuVwXyZaBcDeFgHiJkLmN0pQrS2t4u6v8wAz}
jwt.expiration=${JWT_EXPIRATION:43200000}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:2592000000}

# Email Configuration
spring.mail.host=${SMTP_HOST:mailcluster.loopia.se}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USER:info@duggalsfastigheter.se}
spring.mail.password=${SMTP_PASS:!e8E$..n2MP2W_7}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Security Configuration
spring.security.user.name=${ADMIN_USER:admin}
spring.security.user.password=${ADMIN_PASSWORD:admin}

# Logging Configuration
logging.level.se.duggals.dfrm=${LOG_LEVEL:INFO}
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Application Configuration
app.name=DFRM
app.version=4.1.0
app.environment=${NODE_ENV:development}

# CORS Configuration
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000,https://dfrm.duggalsfastigheter.se}

# Rate Limiting
app.rate-limit.window=${RATE_LIMIT_WINDOW:900000}
app.rate-limit.max-requests=${RATE_LIMIT_MAX_REQUESTS:100}

# File Upload Configuration
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${MAX_FILE_SIZE:10MB}

# Health Check Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized 